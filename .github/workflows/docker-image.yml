name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker login -u ${{ secrets.USERNAME }} -p ${{ secrets.PASSWORD }} && docker build . --file Dockerfile --tag akubukanayam/opium && docker push akubukanayam/opium:latest
